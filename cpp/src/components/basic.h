#include "common/models/Pt.h"
using commmon::models::Pt;

namespace components::basic {
using Orientation = double;
using Speed = double;
using Key = int;
using Key = int;

struct NextPosDef {
  Pt pt;
  bool completed;
};
}  // namespace components::basic
