#include "models/Pt.h"
using models::Pt;

namespace components::basic {
using Orientation = double;
using Speed = double;
using Key = int;
using Key = int;

struct NextPosDef {
  Pt pt;
  bool completed;
};
}  // namespace components::basic
