#include <models/Pt.hpp>

namespace components::basic {
using Orientation = double;
using Speed = double;
using Key = int;
using Key = int;

struct NextPosDef {
  models::Pt pt;
  bool completed;
};
}  // namespace components::basic
